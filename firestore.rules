rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in users may read or write. The app must authenticate
    // (Firebase Auth) before any Firestore access will succeed.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
