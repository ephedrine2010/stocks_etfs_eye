{
  "name": "stocks-eye-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin proxy for the Stocks Eye Flutter app — holds the DeepSeek key server-side and (for the web build) forwards CORS-blocked price/news requests.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
